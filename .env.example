# PostgreSQL credentials
POSTGRES_USER=kpi
POSTGRES_PASSWORD=kpi
POSTGRES_DB=kpi_portal

# Used by the backend service
DATABASE_URL=postgresql+asyncpg://kpi:kpi@db:5432/kpi_portal

# Set this in your frontend JS CONFIG.apiBase after Railway deployment
# RAILWAY_API_URL=https://your-backend-service.up.railway.app
